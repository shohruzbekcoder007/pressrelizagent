name: pressreliz
version: "0.3.0"
description: >
  PressRelizAgent tools. Registers the `pressreliz` toolset. `statind_code`
  maps a plain-language statistical indicator name to its official classifier
  code, searching the Neo4j indicator register and verifying every code the
  model returns.
author: PressRelizAgent
kind: standalone
provides_tools:
  - statind_code
